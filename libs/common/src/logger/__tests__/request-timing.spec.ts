describe('request-timing', () => {
    it.todo('동시 진행되는 두 request 가 각자의 시작 시각을 독립적으로 가진다 (WeakMap 격리)')
    it.todo(
        'markRequestStart 를 같은 request 에 두 번 호출하면 두 번째 시각으로 덮어쓴다 (idempotent override)'
    )
})
