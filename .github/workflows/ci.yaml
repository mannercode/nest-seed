# https://docs.github.com/en/actions/learn-github-actions/contexts#github-context
name: CI

on:
    # Runs when a pull request targeting the main branch is created or updated
    pull_request:
        branches: ['main']
    # Runs when a pull request is merged into the main branch
    push:
        branches: ['main']

jobs:
    test-coverage:
        runs-on: ubuntu-24.04-arm
        container: node:24-bookworm
        env:
            WORKSPACE_ROOT: ${{ github.workspace }}
            TERM: xterm # This is required to enable use of the `clear` command in bash
        steps:
            - uses: actions/checkout@v4

            - name: Install Tools
              run: apt-get update && apt-get install -y curl jq iproute2 sudo

            - name: Setup sudo
              run: echo "node ALL=(root) NOPASSWD:ALL" > /etc/sudoers.d/node && chmod 0440 /etc/sudoers.d/node

            - name: Install Docker
              run: |
                  apt-get install -y ca-certificates curl gnupg
                  install -m 0755 -d /etc/apt/keyrings
                  curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
                  chmod a+r /etc/apt/keyrings/docker.gpg
                  echo \
                    "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
                    $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
                    tee /etc/apt/sources.list.d/docker.list > /dev/null
                  apt-get update
                  apt-get install -y docker-ce-cli

            - name: Map host.docker.internal in job container
              run: |
                  GW_IP=$(ip route | awk '/default/ {print $3}')
                  echo "$GW_IP host.docker.internal" | tee -a /etc/hosts

            - name: Run infrastructures
              run: bash infra/local/reset-infra.sh

            - name: Install deps
              run: npm ci

            - name: Lint
              run: npm run lint

            - name: Run unit tests
              run: npm test

            - name: Run e2e tests
              run: npm run test:e2e
