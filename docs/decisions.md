# 설계 결정

이 문서는 분산 협력 도구를 **왜 골랐는지** 설명합니다. 비슷한 일을 할 수 있지만 **쓰지 않기로 한 도구**도 함께 정리합니다. 각 도구를 어디서 쓰는지는 [architecture.md §2](architecture.md#2-분산-협력--msa-ready-monolith)에 있습니다.

---

## 1. 분산 락: `cache.withLock`와 `withLockBlocking`

### 결정

여러 API 컨테이너가 같은 상태를 동시에 바꾸려는 곳은 Redis 분산 락으로 보호합니다. 구현에는 Redis `SET NX`와 토큰 기반 Lua `DEL`을 사용합니다. 락 사용 방식은 두 가지로 나누었습니다.

- `withLock` — 락을 획득하지 못하면 바로 포기합니다.
- `withLockBlocking` — 락이 해제될 때까지 짧은 간격으로 기다립니다. 너무 오래 기다리면 예외를 던집니다.

### 두 방식의 선택 기준

기준은 _다른 요청이 이미 같은 일을 처리 중일 때 이번 요청을 어떻게 다룰 것인가_ 입니다.

| 상황                                                              | 선택               |
| ----------------------------------------------------------------- | ------------------ |
| 한 번만 실행되면 충분함. 예: 같은 cron이 여러 컨테이너에서 실행됨 | `withLock`         |
| 들어온 요청은 모두 처리하되, 한 번에 하나씩만 처리해야 함         | `withLockBlocking` |

건너뛰어도 사용자에게 영향이 없다면 `withLock`이면 충분합니다. 반면 중복 구매를 막거나 사가의 검증-삽입 경쟁을 막는 곳에서는 요청을 버릴 수 없습니다. 이런 곳에서는 `withLockBlocking`으로 순서대로 처리합니다.

### 검토했던 대안

- **Mongo 트랜잭션이나 `findOneAndUpdate` 필터** — 한 문서의 상태를 한 번에 바꾸는 일에는 좋습니다. 하지만 검증 후 삽입처럼 여러 단계를 묶어야 할 때는 트랜잭션을 오래 유지해야 합니다. 실패했을 때 복구 비용도 커집니다.
- **Redlock** — Redis 마스터 여러 대를 전제로 한 분산 락입니다. 이 시드는 이미 안정적인 Redis 클러스터 한 곳을 사용합니다. 키 하나에 `SET NX`를 거는 방식이면 충분합니다.
- **Temporal task queue의 `concurrency: 1`** — 큐 전체의 동시 실행 수를 1로 묶을 수 있습니다. 하지만 우리는 전체 큐가 아니라 **같은 키**만 순서대로 처리하고 싶습니다. 이 옵션으로는 키별 직렬화를 만들 수 없습니다.

---

## 2. 컨테이너 사이 메시지: NATS pub/sub

### 결정

한 프로세스에서 다른 프로세스로 이벤트를 보내야 하는 곳은 NATS pub/sub을 사용합니다. 앱 코드에서는 NATS를 직접 쓰지 않고, NestJS 제공자로 감싼 `NatsPubSubService`를 통해서만 사용합니다. 큐 그룹 옵션도 함께 열어 두어, 필요하면 큐처럼 동작하게 만들 수 있습니다.

### 근거

API는 기본 4개 컨테이너로 동작합니다. NestJS의 `EventEmitter2`는 같은 프로세스 안에서만 이벤트를 전달합니다. Temporal 워커가 만든 사가 진행 이벤트를 다른 컨테이너에 붙은 SSE 클라이언트에게 보내려면, 컨테이너 사이 메시지 통로가 필요합니다.

NATS를 고른 이유는 한 도구로 여러 동작을 처리할 수 있기 때문입니다.

- 같은 subject를 그대로 구독하면 모든 컨테이너가 이벤트를 받습니다.
- 큐 그룹을 붙이면 같은 그룹 안에서 한 컨테이너만 이벤트를 받습니다.
- JetStream 컨슈머를 쓰면 메시지를 저장하는 큐처럼 확장할 수도 있습니다.

Redis pub/sub은 모든 구독자에게 보내는 동작에는 충분합니다. 하지만 큐처럼 써야 하면 BullMQ 같은 도구가 또 필요합니다. 도구가 늘면 선택 기준과 운영 부담도 함께 늘어납니다.

운영 면에서도 NATS는 클러스터링이 단순합니다. subject를 계층 구조로 만들 수 있어서 메시지 경로도 읽기 쉽습니다.

### 검토했던 대안

- **`EventEmitter2` 그대로 사용** — 다른 컨테이너로 이벤트가 가지 않습니다. SSE 시나리오가 성립하지 않습니다.
- **Redis Pub/Sub** — 모든 구독자에게 뿌리는 용도에는 잘 맞습니다. 하지만 큐처럼 동작해야 할 때는 BullMQ를 따로 들여야 합니다.
- **sticky session(NGINX)** — 클라이언트를 한 컨테이너에 계속 붙이는 방법입니다. 하지만 이벤트를 만드는 워커가 다른 컨테이너에 있을 수 있으므로 근본적인 해결책이 아닙니다.
- **Kafka** — 운영해야 할 구성요소가 많습니다. 지금 시드 규모에는 과합니다.

---

## 3. Saga 오케스트레이션: Temporal 워크플로

### 결정

시간이 오래 걸리거나, 중간 실패 시 보상 작업이 필요한 일은 Temporal 워크플로와 액티비티로 표현합니다. 워크플로 함수는 결정적으로 작성하고, DB 쓰기나 외부 API 호출 같은 부수효과는 액티비티로 분리합니다.

### 근거

이전 showtime-creation 구현은 BullMQ 큐 위에 직접 만든 상태 머신과 수동 `compensate()` 함수로 되어 있었습니다. 사가 단계가 하나뿐일 때는 감당할 수 있었지만, 단계가 늘면 문제가 빠르게 커집니다.

1. **상태 누락이 쉽습니다** — 단계마다 status를 emit해야 SSE가 이어집니다. 한 곳이라도 빠지면 클라이언트가 계속 기다립니다.
2. **재시도와 멱등성을 직접 챙겨야 합니다** — 컨테이너가 종료되면 처리 중이던 작업이 불완전한 상태로 남을 수 있습니다. BullMQ의 ack만으로는 일부만 진행된 작업을 자동으로 보상할 수 없습니다.
3. **진행 상황을 밖에서 보기 어렵습니다** — 단계가 코드 안에만 있습니다. 운영 중 “지금 어디까지 갔나?”를 보려면 로그를 따라가야 합니다.

Temporal로 옮기면 부담이 줄어듭니다.

- 워크플로 실행 기록이 자동으로 저장됩니다. 워커가 종료되어도 다른 워커가 이어받습니다.
- 액티비티별 재시도 규칙을 코드로 적을 수 있습니다.
- 복구 동작은 워크플로의 `try/catch` 안에서 보상 액티비티를 부르면 됩니다. 별도 상태 머신을 직접 만들 필요가 없습니다.
- Temporal Web UI에서 워크플로의 현재 위치와 실행 기록을 볼 수 있습니다.

### 트레이드오프

- 워크플로 코드는 샌드박스에서 실행됩니다. 같은 입력이면 항상 같은 결과가 나와야 합니다. `Date.now()`, 랜덤, 외부 I/O, NestJS DI처럼 실행마다 결과가 달라질 수 있는 것은 워크플로 안에서 직접 쓰지 않고 액티비티로 빼야 합니다.
- 워크플로 번들링은 webpack 기반 `bundleWorkflowCode`로 이뤄집니다. 워크플로 파일이 가져온 모듈의 하위 모듈까지 함께 묶입니다. NestJS 데코레이터가 있는 모듈을 가져오면 번들이 깨질 수 있습니다. 그래서 status enum 같은 값도 NestJS 모듈에 두지 않고 string literal로 직접 적습니다.
- 인프라가 늘어납니다. Temporal server와 backend PostgreSQL이 함께 떠야 합니다. 개발과 테스트에서는 auto-setup 이미지 덕분에 부담이 작지만, 운영에서는 관리 대상이 늘어납니다.

### 검토했던 대안

- **BullMQ에 수동 compensate를 계속 사용** — 사가 단계가 늘수록 보상 처리, 재시도, 상태 관리를 직접 챙겨야 합니다.
- **NATS JetStream 컨슈머** — 메시지 저장과 재시도는 가능합니다. 하지만 사가의 보상과 상태 머신은 직접 만들어야 합니다. 워크플로라는 추상이 없습니다.

---

## 4. 명시적으로 거부한 도구

도입을 검토했지만 시드에는 넣지 않기로 한 도구입니다.

| 도구              | 거부 사유                                                                                                                         |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| Kafka             | NATS로 충분합니다. broker와 controller를 여러 개 운영해야 하고, 토픽을 미리 만들어야 하는 부담이 큽니다.                          |
| BullMQ            | Temporal로 대체했습니다. 사가의 보상 처리·재시도·상태 머신을 직접 작성해야 하는 부담을 워크플로가 줄여 줍니다.                    |
| OpenAPI / Swagger | bash + curl로 만든 실행 가능한 API 문서(`apps/api/api-docs/*.spec`)로 대신합니다. 문서가 실제 동작과 다르면 빌드가 깨지는 방식입니다. |
| Passport          | NestJS의 Guard 인터페이스(`CanActivate`)를 직접 구현해도 충분합니다. 코드가 더 짧고 흐름이 바로 보입니다.                         |
| Service Mesh      | 지금 시드에는 과합니다. Kubernetes 운영으로 옮기는 시점에 다시 검토할 만합니다.                                                   |
