{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Tests",
            "detail": "Run the selected test suite",
            "type": "shell",
            "command": "npm run test:${input:suites}"
        }
    ],
    "inputs": [
        {
            "id": "suites",
            "type": "pickString",
            "options": ["src", "apps", "common", "e2e"],
            "description": "Select a test suite."
        }
    ]
}
