{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Tests",
            "detail": "run npm test with TEST_ROOT",
            "group": { "kind": "test", "isDefault": true },
            "type": "shell",
            "options": { "env": { "TEST_ROOT": "${input:TestRoots}" } },
            "command": "npm test"
        },
        {
            "label": "Run E2E Tests",
            "detail": "npm run test:e2e",
            "type": "shell",
            "command": "npm run test:e2e"
        },
        {
            "label": "Repeat Tests",
            "detail": "repeat selected test command",
            "type": "shell",
            "command": "bash",
            "args": [
                "-lc",
                "set -e; for ((i=1; i<=${input:RepeatCount}; i++)); do echo \"[Run $i/${input:RepeatCount}]\"; ${input:TestCommand}; done; echo \"Done. ${input:RepeatCount} run(s) finished.\""
            ]
        }
    ],
    "inputs": [
        {
            "id": "TestRoots",
            "type": "pickString",
            "options": ["src", "src/apps", "src/libs/common"],
            "description": "Select the source root."
        },
        {
            "id": "RepeatCount",
            "type": "promptString",
            "default": "100",
            "description": "How many times to repeat npm test."
        },
        {
            "id": "TestCommand",
            "type": "pickString",
            "options": ["npm test", "npm run test:e2e"],
            "description": "Select the test command to repeat."
        }
    ]
}
