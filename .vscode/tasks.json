{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Test Unit",
            "detail": "Run unit tests for the selected suite",
            "type": "shell",
            "command": "bash scripts/run-test.sh ${input:tests} 1"
        },
        {
            "label": "Test E2E",
            "detail": "Build Docker and run E2E tests",
            "type": "shell",
            "command": "bash test/e2e/run.sh"
        }
    ],
    "inputs": [
        {
            "id": "tests",
            "type": "pickString",
            "options": ["all", "apps", "common"],
            "description": "Select a unit test suite to run."
        }
    ]
}
