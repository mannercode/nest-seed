services:
    temporal-postgresql:
        extends: { file: ../compose.common.yml, service: common-options }
        image: $TEMPORAL_POSTGRES_IMAGE
        environment:
            POSTGRES_USER: temporal
            POSTGRES_PASSWORD: temporal
            POSTGRES_DB: temporal
        volumes:
            - temporal_pgdata:/var/lib/postgresql/data
        healthcheck:
            test: ['CMD-SHELL', 'pg_isready -U temporal']
        deploy: { resources: { limits: { memory: 2g } } }
        shm_size: '256m'

    # `temporal` / `temporal_visibility` 두 DB 의 스키마를 만든다. `create`
    # 실패는 무시하고 `update` 만 마저 돌려 재실행에도 안전.
    temporal-setup:
        extends: { file: ../compose.common.yml, service: common-options }
        image: $TEMPORAL_ADMIN_TOOLS_IMAGE
        depends_on:
            temporal-postgresql: { condition: service_healthy }
        environment:
            # SQL_PASSWORD 는 temporal-sql-tool, POSTGRES_PWD 는 다른 도구가
            # 읽는다. 둘 다 같은 값이어야 통과.
            DB: postgres12
            DB_PORT: '5432'
            POSTGRES_USER: temporal
            POSTGRES_PWD: temporal
            POSTGRES_SEEDS: temporal-postgresql
            SQL_PASSWORD: temporal
        volumes:
            - ./scripts:/scripts:ro
        entrypoint: ['/bin/sh']
        command: '/scripts/setup-postgres.sh'

    temporal:
        extends: { file: ../compose.common.yml, service: common-options }
        image: $TEMPORAL_SERVER_IMAGE
        depends_on:
            temporal-setup: { condition: service_completed_successfully }
        environment:
            DB: postgres12
            DB_PORT: '5432'
            POSTGRES_USER: temporal
            POSTGRES_PWD: temporal
            POSTGRES_SEEDS: temporal-postgresql
            BIND_ON_IP: 0.0.0.0
            DYNAMIC_CONFIG_FILE_PATH: config/dynamicconfig/development-sql.yaml
        volumes:
            - ./dynamicconfig:/etc/temporal/config/dynamicconfig:ro
        healthcheck:
            test: ['CMD-SHELL', 'nc -z localhost 7233']
            interval: 5s
            start_period: 30s
        # 한도 1g 에서 transfer-queue / timer-queue 가 "context deadline
        # exceeded" 로 깨지는 것을 보고 3g 로 올렸다.
        deploy: { resources: { limits: { memory: 3g } } }
        shm_size: '16m'

    # auto-setup 이미지는 default namespace 를 만들어 줬지만 server 전용
    # 이미지는 그렇지 않다. admin-tools 로 한 번 만든다 (이미 있으면 통과).
    temporal-create-namespace:
        extends: { file: ../compose.common.yml, service: common-options }
        image: $TEMPORAL_ADMIN_TOOLS_IMAGE
        depends_on:
            temporal: { condition: service_healthy }
        environment:
            TEMPORAL_ADDRESS: temporal:7233
            DEFAULT_NAMESPACE: default
        volumes:
            - ./scripts:/scripts:ro
        entrypoint: ['/bin/sh']
        command: '/scripts/create-namespace.sh'

volumes:
    temporal_pgdata: {}
