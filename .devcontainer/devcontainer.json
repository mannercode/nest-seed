{
    "name": "${containerWorkspaceFolderBasename}",
    "hostRequirements": { "cpus": 4, "memory": "16gb", "storage": "32gb" },
    "build": { "dockerfile": "Dockerfile", "context": "." },
    "features": {
        "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {},
        "ghcr.io/devcontainers/features/github-cli:1": {}
    },
    "runArgs": [
        "--env-file",
        ".devcontainer/infra/.env",
        "--name=${localEnv:USER:unknown}-${localWorkspaceFolderBasename}",
        "--network=nest-seed-infra"
    ],
    "remoteUser": "node",
    "containerEnv": { "WORKSPACE_ROOT": "${containerWorkspaceFolder}" },
    // 호스트 파일시스템 노출 없이 source code 자체를 named volume 에 둔다.
    // dod 환경에서 다른 컨테이너 (compose 서비스, build context 등) 가 같은
    // volume 을 mount 하면 호스트 경로 없이 같은 코드를 본다. 첫 부팅은 VSCode
    // 명령 팔레트의 "Dev Containers: Clone Repository in Named Container Volume"
    // 으로 volume name (= nest-seed) 지정해서 git clone — 그 명령이 알아서
    // workspaceMount 를 inject 하므로 여기 명시하지 않는다.
    "workspaceFolder": "/workspaces/nest-seed",
    "mounts": [
        "source=${localEnv:HOME}/.claude,target=/home/node/.claude,type=bind",
        "source=${localEnv:HOME}/.claude.json,target=/home/node/.claude.json,type=bind",
        "source=${localEnv:HOME}/.config,target=/home/node/.config,type=bind"
    ],
    "initializeCommand": {
        "prepare-mounts": "mkdir -p ${localEnv:HOME}/.claude ${localEnv:HOME}/.config",
        "prepare-files": "touch ${localEnv:HOME}/.claude.json",
        "prepare-network": "docker network create nest-seed-infra 2>/dev/null || true"
    },
    // "postCreateCommand": { "husky": "husky", "install": "npm install" },
    // "postStartCommand": {
    //     "infra": "bash .devcontainer/infra/reset.sh",
    //     "plantuml": "nohup bash -c 'java -jar /opt/plantuml.jar -picoweb:5020 >/tmp/plantuml.log &' >/dev/null"
    // },
    // "waitFor": "postStartCommand",
    "forwardPorts": [5010, 5020, 9001],
    "customizations": {
        "vscode": {
            "extensions": [
                "dbaeumer.vscode-eslint",
                "firsttris.vscode-jest-runner",
                "ms-vscode.live-server",
                "ms-azuretools.vscode-docker",
                "ms-azuretools.vscode-containers",
                "jebbs.plantuml",
                "esbenp.prettier-vscode",
                "mutantdino.resourcemonitor",
                "mkhl.shfmt",
                "anthropic.claude-code"
            ],
            "settings": {
                "remote.autoForwardPorts": false,
                "remote.downloadExtensionsLocally": true,
                "livePreview.portNumber": 5010,
                "plantuml.server": "http://localhost:5020/",
                "plantuml.render": "PlantUMLServer"
            }
        }
    }
}
