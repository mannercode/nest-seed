# 저장소 작업 지침

이 파일은 저장소 전체에 적용된다. 세부 설계와 실행 방법을 여기에 복제하지 말고, 작업 범위에 맞는 문서를 먼저 읽는다.

## 1. 문서를 먼저 읽는다

모든 작업은 [README.md](README.md)에서 시작한다. 다음 영역을 변경하기 전에는 대응하는 문서를 읽고 그 경계와 보장을 유지한다.

- `apps/`: [애플리케이션](docs/apps.md), [설계 결정](docs/reference/decisions.md)
- `libs/`: [공유 패키지](docs/libs.md), [개발 규칙](docs/reference/conventions.md)
- `tests/`: [외부 스택 테스트](docs/tests.md), [실행 방법](tests/README.md)
- `infra/`: [개발 인프라](docs/infra.md), [환경 변수](docs/reference/environment.md)
- `deploy/`: [검증 배포](docs/deploy.md)
- `.devcontainer/`: [개발 환경](docs/devcontainer.md)

문서와 코드가 충돌하고 어느 쪽이 의도인지 판단해야 한다면 임의로 한쪽을 맞추지 말고 작업을 중단해 사용자에게 묻는다.

## 2. fallback과 꼼수를 만들지 않는다

요구사항에 없는 fallback, 조용한 기본값, 실패를 성공처럼 바꾸는 예외 처리, 중복 대체 경로를 추가하지 않는다. 필요해 보이면 구현하기 전에 사용자에게 필요성과 영향을 설명하고 결정을 요청한다.

문제를 통과시키기 위해 다음과 같은 우회는 하지 않는다.

- 테스트·타입 검사·lint·커버리지 기준을 끄거나 낮추기
- 실패하는 검증을 skip하거나 assertion을 약하게 바꾸기
- 테스트만 통과하도록 결과를 하드코딩하거나 운영 코드에 테스트 전용 분기 넣기
- race condition과 lifecycle 오류를 임의의 sleep·retry·timeout 증가로 숨기기
- 기존 경계를 피해 같은 기능을 다른 위치에 중복 구현하기

원인을 현재 설계 경계 안에서 직접 해결한다. 올바른 해결에 범위 확장이나 설계 변경이 필요하면 우회하지 말고 사용자 결정을 받는다.

## 3. 필요한 것이 없으면 요청한다

요구사항만으로 API 계약, 데이터 모델, 보안 경계, 정합성 보장, 새 의존성처럼 결과를 바꾸는 결정을 내릴 수 없으면 작업을 중단한다. 확인한 사실, 선택지와 영향, 추천안을 간결하게 제시하고 사용자에게 묻는다.

필요한 secret, 권한, 외부 서비스, 원본 데이터, 도구가 없으면 가짜 값이나 다른 수단으로 대체하지 않는다. 무엇이 왜 필요한지 명시해 요청한다.

## 4. 보안을 선제 구현하지 않는다

명시적으로 요청된 범위에 필요한 최소한의 보안만 구현한다. 보안을 이유로 마스킹, 암호화, 접근 제한, 추가 유효성 검사, 네트워크 차단, secret 관리 또는 그에 따른 테스트와 설정을 임의로 추가하지 않는다.

요구사항을 구현하면서 보안상 우려되는 부분을 발견하면 먼저 영향과 선택지를 설명하고 사용자에게 결정을 요청한다. 에이전트가 위협 모델이나 보안 수준을 대신 정하지 않는다.

## 5. 요구사항만 최소한으로 수행한다

명시된 요구사항을 정확히 충족하는 가장 작은 변경을 만든다. 요청하지 않은 기능 추가, 선제적 추상화, 광범위한 refactoring, 파일 이동, 의존성 교체, 주변 코드 정리를 함께 벌이지 않는다.

요구사항을 안전하게 완성하는 데 필요한 테스트·타입·문서 변경은 같은 범위에 포함한다. 그 밖의 문제를 발견하면 현재 작업의 정확성을 막지 않는 한 수정하지 말고 별도로 알린다. 어디까지가 필수인지 판단에 따라 결과가 달라지면 먼저 사용자에게 묻는다.

테스트 파일을 임의로 추가하지 않는다. 새 테스트 파일이 필요하다고 판단하면 작성하기 전에 사용자 동의를 구한다.

문서는 간결하게 작성한다. 코드·설정·파일명만으로 알 수 있는 내용, 당연한 설명과 작업 이력은 적지 않고 코드에서 드러나지 않는 목적·결정·제약만 남긴다.

## 6. 변경을 검증한다

사용자의 기존 변경과 작업 범위 밖의 파일을 보존한다. 관련 검사를 실제로 실행하고, 실행하지 못한 검사는 이유를 알린다. 검사 통과를 위해 계약을 바꾸지 않는다.

동작이나 보장이 바뀌면 해당 테스트와 그 의미를 소유하는 문서를 함께 갱신한다.

## 7. 커밋 요청은 push와 merge까지 뜻한다

사용자가 `커밋`을 요청하면 추가 확인 없이 변경을 검증해 커밋하고 원격 저장소에 push한 뒤, 저장소의 정상 절차와 필수 검사를 거쳐 기본 브랜치에 merge까지 완료한다. 작업 브랜치가 이미 기본 브랜치라면 해당하지 않는 merge 단계만 생략한다.

보호 규칙, 실패한 필수 검사, merge conflict, 권한 부족으로 완료할 수 없으면 이를 우회하지 않는다. 확인한 상태와 필요한 조치를 사용자에게 요청한다.
