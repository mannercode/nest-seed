# 단순화 작업 계획

`apps/api`와 `libs/`가 시드의 핵심이다. 나머지는 이를 실행하고 보여 주는 데 필요한 정도로 둔다.

## 진행 범위

- **빈 조회 조건** — `QueryBuilder.addIn`은 미지정과 빈 목록을 구분한다. 빈 목록으로 조회 범위가 넓어지거나 필수 필터 오류가 나지 않게 한다.
- **공통 런타임의 테스트 분기** — Restate의 포트·로거와 MongoDB 연결 옵션을 테스트 실행 여부로 바꾸지 않는다. fixture가 필요한 값을 명시하며 연결 공유 범위와 종료 책임은 유지한다.
- **인증 확장 지점** — 사용하지 않는 이벤트 callback·컨텍스트·사용자 ID 경로 설정과 중복 만료 검사를 줄인다. 로그인·갱신·로그아웃, 5분 액세스 토큰, 리프레시 토큰의 원자 교체는 유지한다.
- **중복 단위테스트** — 로거 위임·인증 helper·연결 옵션의 구현 복제 검증을 기존 통합·연동 테스트에 모은다. 오류 문구 전체를 복제하는 대신 클라이언트가 구분하는 공개 오류 코드 계약만 남긴다.
- **한 구매에 한 상영** — 여러 상영에 걸친 순차 claim과 rollback을 제거한다. 한 상영 안의 원자 선점, 소유권 검사, 부분 구매 후 남은 선점의 TTL은 유지한다.
- **구매 복구를 Restate로 통일** — 자체 lease·주기 재조정·미해소 결제 검색을 workflow로 대체한다. 구매의 동기 HTTP 응답, DB transaction과 최초 응답 저장, 멱등 결제·보상, JetStream 전달은 유지한다. 알림 발행은 별도 workflow에서 복구한다.

## 유지·유보

- **단건·다건 API는 변경하지 않는다.** `getMany`·`deleteMany` 계약과 해당 네이밍 규칙을 유지한다.
- **JSON·DTO 자동 변환은 유보한다.** [기존 검토](../docs/review/runtime-complexity-review.md)를 기준으로 일반 JSON 파싱, 필드별 변환, Restate·메시지의 Temporal 복원 계약을 구분한 뒤 결정한다.
- 데모의 프록시·쿠키 처리는 각 Next.js 앱이 소유한다. 기존 공용 유틸과 외부 연동 경계는 유지한다.

## 완료 확인

- [x] 구매·선점·빈 상영 조회를 기존 API 통합테스트로 검증한다.
- [x] 결제·DB 응답 유실, 실패 보상, 알림 재전송과 복제본 경합을 검증한다.
- [x] 타입·lint·100% 커버리지와 API 문서를 검증한다.
- [x] 현재 계약을 소유한 문서를 구현과 맞춘다. 복구한 과거 원문은 갱신하지 않는다.
