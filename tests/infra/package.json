{
    "name": "infra-tests",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "lint": "oxlint -c ../../oxlint.json restate-journal-recovery.js && prettier --check restate-journal-recovery.js package.json",
        "format": "prettier --write restate-journal-recovery.js package.json",
        "atoz": "pnpm run lint && node --test --test-reporter=spec restate-journal-recovery.js"
    },
    "devDependencies": {
        "@restatedev/restate-sdk": "1.17.0",
        "@restatedev/restate-sdk-clients": "1.17.0"
    }
}
