{
    "name": "api-tests",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "lint": "oxlint -c ../../oxlint.json race/ benchmark/ && prettier --check 'race/*.js' 'benchmark/*.js' package.json",
        "format": "prettier --write 'race/*.js' 'benchmark/*.js' package.json",
        "atoz": "pnpm run lint"
    }
}
