{
    "name": "api-race",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "lint": "oxlint -c ../../oxlint.json . && prettier --check '*.js' 'probes/*.js' package.json",
        "format": "prettier --write '*.js' 'probes/*.js' package.json",
        "atoz": "pnpm run lint"
    },
    "devDependencies": {
        "@restatedev/restate-sdk": "1.17.0",
        "@restatedev/restate-sdk-clients": "1.17.0"
    }
}
