API_BASE_URL=http://localhost:3000

# dev 환경에서 로그인/가입 폼을 자동 채울 값. 가입 페이지에서 같은 값으로
# 한 번 가입하면 로그인 페이지도 같은 값으로 곧바로 동작한다.
# 프로덕션 빌드에서는 자동 채움이 동작하지 않는다.
NEXT_PUBLIC_DEV_LOGIN_EMAIL=dev@nest-seed.local
NEXT_PUBLIC_DEV_LOGIN_PASSWORD=DevPass1!
NEXT_PUBLIC_DEV_SIGNUP_NAME=Dev User
