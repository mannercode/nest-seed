API_BASE_URL=http://localhost:3000

# dev 환경에서 콘솔 로그인 폼을 자동 채울 값. apps/api/src/bootstrap.ts의
# dev 시드 admin과 동일하게 둔다. 프로덕션 빌드에서는 자동 채움이 동작하지 않는다.
NEXT_PUBLIC_DEV_LOGIN_EMAIL=admin@nest-seed.local
NEXT_PUBLIC_DEV_LOGIN_PASSWORD=DevPass1!
